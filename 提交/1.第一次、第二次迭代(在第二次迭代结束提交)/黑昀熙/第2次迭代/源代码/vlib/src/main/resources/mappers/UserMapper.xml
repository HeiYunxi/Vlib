<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fzsx.vlib.mapper.UserMapper">
    <select id="UserInfo" resultType="com.fzsx.vlib.entity.User">
        select * from user
    </select>
    <!--用户信息注册-->
    <insert id="saveUserInfo" parameterType="com.fzsx.vlib.entity.User">
        insert into user(userName,account,email,password,intro,phone,create_time,update_time,type)
        values(#{userName}, #{account}, #{email}, #{password},#{intro},#{phone},#{create_time},#{update_time},#{type});
    </insert>

    <select id="getUserByAccountName" resultType="com.fzsx.vlib.entity.User">
        select userId,userName,phone,account from user where account = #{value}
    </select>

    <select id="getUserByLoginInfo" resultType="com.fzsx.vlib.entity.User">
        select userId,userName,phone,account,create_time,type
        from user
        where account = #{account} and password = #{password}
    </select>

    <!--用户信息分页查询-->
    <select id="queryUserList" parameterType="com.fzsx.vlib.entity.User" resultType="com.fzsx.vlib.entity.User">
        select * from user
        <where>
            <if test="account != null and account != ''">
                and account like concat('%', #{account}, '%')
            </if>
        </where>
    </select>

    <!--删除用户-->
    <delete id="delete" parameterType="com.fzsx.vlib.entity.User">
        delete from user where userId = #{userId}
    </delete>

    <!--查询用户明细-->
    <select id="detail" parameterType="com.fzsx.vlib.entity.User" resultType="com.fzsx.vlib.entity.User">
        select * from user
        <where>
            <if test="userId != null and userId != ''">
                and userId = #{userId}
            </if>
        </where>
    </select>

    <!--更新用户信息-->
    <update id="update" parameterType="com.fzsx.vlib.entity.User">
        update user
        set
        <if test="userName != null and userName != ''">
            userName = #{userName},
        </if>
        <if test="account != null and account != ''">
            account = #{account},
        </if>
        <if test="email != null and email != ''">
            email = #{email},
        </if>
        <if test="password != null and password != ''">
            password = #{password},
        </if>
        <if test="intro != null and intro != ''">
            intro =#{intro},
        </if>
        <if test="phone != null and phone != ''">
            phone =#{phone},
        </if>
        <if test="create_time != null">
            create_time =#{create_time},
        </if>
        <if test="update_time != null">
            update_time =#{update_time},
        </if>
        <if test="type != null and type != ''">
            type =#{type}
        </if>
        where userId = #{userId};
    </update>
</mapper>
