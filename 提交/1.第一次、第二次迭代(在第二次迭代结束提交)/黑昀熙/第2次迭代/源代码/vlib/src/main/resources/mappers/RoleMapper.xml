<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fzsx.vlib.mapper.RoleMapper">

    <!--角色信息注册-->
    <insert id="saveRoleInfo" parameterType="com.fzsx.vlib.entity.RoleEntity">
        insert into role(roleName,description,create_time,update_time)
        values(#{roleName}, #{description}, #{create_time},#{update_time});
    </insert>

    <!--角色信息分页查询-->
    <select id="queryRoleList" parameterType="com.fzsx.vlib.entity.RoleEntity" resultType="com.fzsx.vlib.entity.RoleEntity">
        select * from role
        <where>
            <if test="roleName != null and roleName != ''">
                and roleName like concat('%', #{roleName}, '%')
            </if>
        </where>
    </select>

    <!--删除角色-->
    <delete id="delete" parameterType="com.fzsx.vlib.entity.RoleEntity">
        delete from role where roleId = #{roleId}
    </delete>

    <!--查询角色明细-->
    <select id="detail" parameterType="com.fzsx.vlib.entity.RoleEntity" resultType="com.fzsx.vlib.entity.RoleEntity">
        select * from role
        <where>
            <if test="roleId != null and roleId != ''">
                and roleId = #{roleId}
            </if>
        </where>
    </select>

    <!--更新角色信息-->
    <update id="update" parameterType="com.fzsx.vlib.entity.RoleEntity">
        update role
        set
        <if test="roleName != null and roleName != ''">
            roleName = #{roleName},
        </if>
        <if test="description != null and description != ''">
            description = #{description},
        </if>
        <if test="create_time != null">
            create_time =#{create_time},
        </if>
        <if test="update_time != null">
            update_time =#{update_time}
        </if>
        where roleId = #{roleId};
    </update>
</mapper>